import { createStore } from 'redux';
import aSimplereducer from './Reducers';

// console.log(todoApp);

const store = createStore(aSimplereducer);
export default store;

// export default store = createStore(aSimplereducer);
